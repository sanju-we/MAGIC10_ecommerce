<%- include('../partials/user/header') %>

<nav class="bg-dark shadow-lg">
    <div class="container mx-auto px-4">
        <!-- Top Level Navigation -->
        <div class="flex justify-between items-center py-4">
            <!-- Logo -->
            <a href="/" class="text-2xl font-bold text-gold-600">MAGIC10</a>
            
            <!-- Desktop Navigation -->
            <div class="hidden lg:flex items-center space-x-8">
                <!-- Main Links -->
                <div class="flex space-x-6">
                    <a href="/" class="hover:text-purple-600 transition">HOME</a>
                    <a href="/shop" class="hover:text-purple-600 transition">SHOP</a>
                    <a href="/about" class="hover:text-purple-600 transition">ABOUT</a>
                    <a href="/contact" class="hover:text-purple-600 transition">CONTACT</a>
                </div>
                
                <!-- Icons -->
                <div class="flex items-center space-x-5 ml-6">
                    <a href="/wishlist" class="text-gray-600 hover:text-purple-600 transition">
                        <i class="fas fa-heart text-xl"></i>
                    </a>
                    <a href="/cart" class="text-gray-600 hover:text-purple-600 transition">
                        <i class="fas fa-shopping-bag text-xl"></i>
                    </a>
                    
                    <!-- User Dropdown -->
                    <div class="relative dropdown">
                        <button class="flex items-center space-x-2 focus:outline-none">
                            <span class="font-medium"><%= user.username %></span>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </button>
                        <div class="dropdown-menu absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg py-1 z-50 hidden">
                            <a href="#" class="block px-4 py-2 hover:bg-purple-50">
                                <i class="fas fa-user mr-2"></i>Dashboard
                            </a>
                            <a href="/orderHistory" class="block px-4 py-2 hover:bg-purple-50">
                                <i class="fas fa-history mr-2"></i>Order History
                            </a>
                            <a href="/userProfile/address/<%=user._id%>" class="block px-4 py-2 hover:bg-purple-50">
                                <i class="fas fa-map-marker-alt mr-2"></i>Address Book
                            </a>
                            <a href="/userProfile/edit/<%=user._id%>" class="block px-4 py-2 hover:bg-purple-50">
                                <i class="fas fa-cog mr-2"></i>Settings
                            </a>
                            <a href="/userProfile/wallet/<%=user._id%>" class="block px-4 py-2 hover:bg-purple-50">
                                <i class="fas fa-wallet mr-2"></i>Wallet
                            </a>
                            <div class="border-t my-1"></div>
                            <a href="/logout" class="block px-4 py-2 text-red-500 hover:bg-red-50">
                                <i class="fas fa-sign-out-alt mr-2"></i>Logout
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Mobile Menu Button -->
            <button id="mobileMenuButton" class="lg:hidden text-gray-600 focus:outline-none">
                <i class="fas fa-bars text-2xl"></i>
            </button>
        </div>
        
        <!-- Mobile Menu (Collapsible) -->
        <div id="mobileMenu" class="mobile-menu lg:hidden">
            <div class="pb-4 space-y-2">
                <a href="/" class="block py-2 px-4 hover:bg-purple-50 rounded" style="color: #fff;">HOME</a>
                <a href="/shop" class="block py-2 px-4 hover:bg-purple-50 rounded" style="color: #fff;">SHOP</a>
                <a href="/about" class="block py-2 px-4 hover:bg-purple-50 rounded" style="color: #fff;">ABOUT</a>
                <a href="/contact" class="block py-2 px-4 hover:bg-purple-50 rounded" style="color: #fff;">CONTACT</a>
                
                <div class="pt-2 border-t">
                    <a href="/wishlist" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-heart mr-3"></i>Wishlist
                    </a>
                    <a href="/cart" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-shopping-bag mr-3"></i>Cart
                    </a>
                </div>
                
                <div class="pt-2 border-t">
                    <a href="/userProfile" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-user mr-3"></i>Dashboard
                    </a>
                    <a href="/orderHistory" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-history mr-3"></i>Order History
                    </a>
                    <a href="/userProfile/address/<%=user._id%>" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-map-marker-alt mr-3"></i>Address Book
                    </a>
                    <a href="/userProfile/edit/<%=user._id%>" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-cog mr-3"></i>Settings
                    </a>
                    <a href="/userProfile/wallet/<%=user._id%>" class="block py-2 px-4 hover:bg-purple-50 rounded">
                        <i class="fas fa-wallet mr-3"></i>Wallet
                    </a>
                    <a href="/logout" class="block py-2 px-4 text-red-500 hover:bg-red-50 rounded">
                        <i class="fas fa-sign-out-alt mr-3"></i>Logout
                    </a>
                </div>
            </div>
        </div>
    </div>
</nav>

    <style>
        .add-address-container {
            max-width: 800px;
            margin: 3rem auto;
            padding: 2rem;
            background: var(--secondary-bg);
            border-radius: 12px;
            border: 1px solid rgba(196, 162, 119, 0.1);
        }

        .address-form .form-group {
            margin-bottom: 1.5rem;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1.5rem;
        }

        .address-type-group {
            display: flex;
            gap: 2rem;
            margin: 1rem 0;
        }

        .address-type-group label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text-primary);
        }

        input[type="radio"] {
            accent-color: var(--accent-gold);
        }

        .default-address {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 1rem 0;
        }

        .save-address-btn {
            width: 100%;
            margin-top: 1.5rem;
        }

        .error-msg {
            color: red;
            font-size: 0.9rem;
            margin-top: 0.2rem;
        }
    </style>

    <div class="add-address-container">
        <h2 class="text-center mb-4">Edit Address</h2>

        <form class="address-form" action="/userProfile/editAddress/<%=addressData._id%>" method="post" id="addAddressForm">
            <div class="form-group">
                <label class="form-label">Full Name</label>
                <input type="text" class="form-control" name="fullName" id="fullName" value=<%=addressData.fullName%> required>
                <p class="error-msg" id="fullNameError"></p>
            </div>

            <div class="form-group">
                <label class="form-label">Phone Number</label>
                <input type="tel" class="form-control" name="phone" value="<%=addressData.phone%>" id="phone" required>
                <p class="error-msg" id="phoneError"></p>
            </div>

            <div class="form-group">
                <label class="form-label">Street Address</label>
                <textarea class="form-control" name="street" id="street" rows="3" required><%= addressData.street %></textarea>
                <p class="error-msg" id="streetError"></p>
            </div>

            <div class="form-grid">
                <div class="form-group">
                    <label class="form-label">City</label>
                    <input type="text" class="form-control" name="city" value="<%=addressData.city%>" id="city" required>
                    <p class="error-msg" id="cityError"></p>
                </div>

                <div class="form-group">
                    <label class="form-label">Land Mark</label>
                    <input type="text" class="form-control" name="LandMark" value="<%=addressData.LandMark%>" id="landmark" required>
                    <p class="error-msg" id="landmarkError"></p>
                </div>
            </div>

            <div class="form-grid">
                <div class="form-group">
                    <label class="form-label">PIN Code</label>
                    <input type="text" class="form-control" name="PINcode" value="<%=addressData.PINcode%>" id="pinCode" required>
                    <p class="error-msg" id="pinCodeError"></p>
                </div>

                <div class="form-group">
                    <label class="form-label">State</label>
                    <select class="form-control" name="State" value="<%=addressData.State%>" id="state" required>
                        <option value="">Select State</option>
                        <option value="Kerala">Kerala</option>
                        <option value="Tamil-Nadu">Tamil Nadu</option>
                        <option value="Karnataka">Karnataka</option>
                    </select>
                    <p class="error-msg" id="stateError"></p>
                </div>
            </div>

            <div class="form-group">
                <label class="form-label">Address Type</label>
                <div class="address-type-group">
                    <label><input type="radio" name="addressType" value="home"> Home</label>
                    <label><input type="radio" name="addressType" value="office"> Office</label>
                    <label><input type="radio" name="addressType" value="other"> Other</label>
                </div>
                <p class="error-msg" id="addressTypeError"></p>
            </div>

            <div class="default-address">
                <input type="radio" id="defaultAddress" name="defaultAddress">
                <label for="defaultAddress" class="form-label">Set as default address</label>
            </div>

            <input type="submit" class="primary-btn" value="Submit">
        </form>
    </div>

    <script>
        function validateFullName() {
            const fullName = document.getElementById('fullName');
            const errorElement = document.getElementById('fullNameError');
            if (!/^[A-Za-z\s]+$/.test(fullName.value)) {
                errorElement.innerText = 'Only letters allowed';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validatePhone() {
            const phone = document.getElementById('phone');
            const errorElement = document.getElementById('phoneError');
            phone.value = phone.value.replace(/\D/g, '');
            if (!/^\d{10}$/.test(phone.value)) {
                errorElement.innerText = 'Must be 10 digits';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validateStreet() {
            const street = document.getElementById('street');
            const errorElement = document.getElementById('streetError');
            if (street.value.trim() === '') {
                errorElement.innerText = 'Required';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validateCity() {
            const city = document.getElementById('city');
            const errorElement = document.getElementById('cityError');
            if (!/^[A-Za-z\s]+$/.test(city.value)) {
                errorElement.innerText = 'Only letters allowed';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validateLandmark() {
            const landmark = document.getElementById('landmark');
            const errorElement = document.getElementById('landmarkError');
            if (landmark.value.trim() === '') {
                errorElement.innerText = 'Required';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validatePinCode() {
            const pinCode = document.getElementById('pinCode');
            const errorElement = document.getElementById('pinCodeError');
            pinCode.value = pinCode.value.replace(/\D/g, '');
            if (!/^\d{6}$/.test(pinCode.value)) {
                errorElement.innerText = 'Must be 6 digits';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validateState() {
            const state = document.getElementById('state');
            const errorElement = document.getElementById('stateError');
            if (state.value === '') {
                errorElement.innerText = 'Required';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        function validateAddressType() {
            const addressType = document.querySelector('input[name="addressType"]:checked');
            const errorElement = document.getElementById('addressTypeError');
            if (!addressType) {
                errorElement.innerText = 'Required';
                return false;
            }
            errorElement.innerText = '';
            return true;
        }

        document.getElementById('fullName').addEventListener('input', validateFullName);
        document.getElementById('phone').addEventListener('input', validatePhone);
        document.getElementById('street').addEventListener('input', validateStreet);
        document.getElementById('city').addEventListener('input', validateCity);
        document.getElementById('landmark').addEventListener('input', validateLandmark);
        document.getElementById('pinCode').addEventListener('input', validatePinCode);
        document.getElementById('state').addEventListener('change', validateState);

        document.querySelectorAll('input[name="addressType"]').forEach(radio => {
            radio.addEventListener('change', validateAddressType);
        });

        document.getElementById('addAddressForm').addEventListener('submit', function (e) {
            e.preventDefault();

            const validations = [
                validateFullName(),
                validatePhone(),
                validateStreet(),
                validateCity(),
                validateLandmark(),
                validatePinCode(),
                validateState(),
                validateAddressType()
            ];

            if (validations.includes(false)) {
                console.log('Validation failed, form not submitted.');
                return;
            }

            console.log('Validation passed, submitting form.');
            this.submit(); 
        });

    </script>

    <%- include('../partials/user/footer') %>